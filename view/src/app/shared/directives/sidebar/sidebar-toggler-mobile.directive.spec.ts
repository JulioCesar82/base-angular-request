import { SidebarTogglerMobileDirective } from './sidebar-toggler-mobile.directive';

describe('SidebarTogglerMobileDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarTogglerMobileDirective();
    expect(directive).toBeTruthy();
  });
});
