import { BrandMinimizedDirective } from './brand-minimized.directive';

describe('BrandMinimizedDirective', () => {
  it('should create an instance', () => {
    const directive = new BrandMinimizedDirective();
    expect(directive).toBeTruthy();
  });
});
